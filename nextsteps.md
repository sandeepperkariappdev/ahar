Next steps

1. First for the existing home page use the global.css files to change the themse, so we cna use the same code architecture across toher pages.

Prompts

1. Now can you create the prompts for the below Login/sing up screen. use the colors and styles from the src/app/globals.css so in the future if I want to change the theme I can change the colors in the src/app/globals.css and the theme of the site changes